# 🤖 Telegram Bot — Scheduled Posting & Channel Management
## Автоматическая публикация и администрирование Telegram-канала
Автор: **Альберт**

---

## 📌 Описание проекта

Данный проект представляет собой Telegram-бота, предназначенного для
автоматической публикации постов в Telegram-канале по заранее заданному
расписанию с возможностью администрирования.

Бот поддерживает:
- планирование публикаций,
- управление постами через админ-интерфейс,
- восстановление всех запланированных задач после перезапуска,
- хранение данных в базе.

Проект рассчитан на использование в production-среде (VPS).

---

## 📁 Структура проекта

```
.
├── handlers/                 # Хендлеры бота (start, admin, user, manage posts)
├── keyboards/                # Inline / Reply клавиатуры
├── utils/                    # Планировщик, БД, вспомогательные утилиты
├── data/                     # Файлы базы данных
├── bot.py                    # Точка входа бота
├── config.py                 # Загрузка конфигурации
├── requirements.txt          # Зависимости проекта
├── .env.example              # Пример переменных окружения
├── README.md                 # Описание проекта
└── LICENSE                   # Лицензия
```

# ⚙️ Конфигурация

### **Все чувствительные параметры выносятся в переменные окружения.
### **Для запуска необходимо создать файл .env на основе .env.example:

```bash
BOT_TOKEN=your_bot_token_here
CHANNEL_ID=@your_channel_username
DB_PATH=data/posts.db
```

**Изменение канала или токена не требует изменения кода — достаточно обновить файл `.env` и перезапустить бота.**

# 🚀 Установка и запуск
```bash
git clone https://github.com/<your-username>/bot-irina-psychologist.git
cd bot-irina-psychologist

python -m venv venv
source venv/bin/activate      # Linux / macOS
venv\Scripts\activate         # Windows

pip install -r requirements.txt

cp .env.example .env

python bot.py
```

## 🖥 Развёртывание

Проект предназначен для запуска на VPS.

### Рекомендуемая среда
- **Ubuntu 22.04 LTS**
- **Python 3.10+**
- использование **virtualenv**
- **systemd** для автозапуска
- обновление через GitHub (`git pull`)

Бот устойчив к перезапускам сервера и автоматически восстанавливает
все запланированные задачи.

---

## 🔒 Безопасность

- файл `.env` не должен попадать в репозиторий
- файлы базы данных не коммитятся
- публично хранится только `.env.example`
- токены и идентификаторы каналов хранятся локально

---

## 📄 Лицензия

Проект распространяется под лицензией **MIT**.
Подробности см. в файле `LICENSE`.
